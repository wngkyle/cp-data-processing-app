import React from "react";
import { useStepsDispatchContext } from "../context/StepContext";

export default function NextButton() {
    return (
        <button>
            Next
        </button>
    )
}